# JavaScript Learning Examples 🚀

This repository contains **step-by-step JavaScript examples** created to understand **core JavaScript concepts from basics to object-oriented programming**.

Each example is written in a **simple, readable way**, with:
- clear comments
- practical examples
- key learning points at the end of most files

This repo is ideal for:
- beginners learning JavaScript  
- automation engineers moving from Java to JavaScript  
- anyone revising JavaScript fundamentals  

---

## 📌 Topics Covered

### 🔹 Basics
- Single-line and multi-line comments
- Variables (`var`, `let`, `const`)
- Dynamic typing in JavaScript
- `typeof` operator
- Data types (Primitive & Non-Primitive)

### 🔹 Operators
- Arithmetic operators (`+`, `-`, `*`, `/`, `%`)
- Increment & decrement (`++`, `--`)
- Compound operators (`+=`, `-=`, etc.)
- Operator precedence
- Logical operators (`&&`, `||`, `!`)

### 🔹 Control Flow
- `if`, `else`
- `for`, `while`, `do...while`
- Nested conditions

### 🔹 Arrays
- Array creation
- `push()`, `pop()`
- `shift()`, `unshift()`
- `slice()` vs `splice()`
- `concat()`
- `reverse()`
- `sort()` (default, custom, ascending & descending)
- Array iteration
- `filter()`, `map()`, `reduce()`
- Chaining (`filter + sort + map`, `filter + sort + reduce`)
- Real-world examples (cart total, top-N calculations)

### 🔹 Strings
- `length`, `charAt()`, indexing
- `slice()`, `split()`
- `indexOf()`, `includes()`
- `trim()`
- `toUpperCase()`, `toLowerCase()`
- `replace()`, `replaceAll()`
- Template literals
- String comparison & normalization
- Regular expressions (basics + practical examples)

### 🔹 Functions
- Function declarations
- Function expressions
- Anonymous functions
- Arrow functions
- Default parameters
- Rest operator (`...`)
- Scope (global vs local)

### 🔹 Objects
- Object creation
- Accessing & updating properties
- Looping with `for...in`
- `Object.keys()`, `Object.values()`, `Object.entries()`
- Objects inside arrays (real-world data)
- Destructuring objects
- Spread operator with objects & arrays

### 🔹 Classes & OOP
- Classes and objects
- Constructors (default & parameterized)
- Multiple objects from a class
- Inheritance (`extends`)
- Method overriding
- `super` keyword
- Exporting classes using `module.exports`
- Importing classes using `require()`

---

## 🧠 How to Use This Repository
1. Clone the repository:
    git clone https://github.com/rajeshyemul/javascript-basics.git
2. Navigate to the project directory and run examples using Node.js:
    node filename.js
3. Read the comments carefully inside each file.
    Most examples include a section
   /*
   Key Learning Points
   */
4. Modify values, experiment with the code, and observe the output to reinforce learning.

## 🎯 Learning Approach
Concepts are introduced gradually
- Examples are practical and easy to understand
- Code is written with real-world usage in mind
- Ideal foundation before moving to:
  - Automation frameworks
  - Frontend libraries
  - Backend development with Node.js
## 👨‍💻 Who Is This For?
- Java developers moving to JavaScript
- Test Automation Engineers
- QA Engineers learning Playwright or Cypress
- Students and self-learners

## 📦 Tech Stack
- JavaScript (ES6+)
- Node.js
- CommonJS modules (require, module.exports)

## 🙌 Contribution
- This is a learning-focused repository.
- You are welcome to:
  - Fork the repository
  - Add new examples
  - Improve explanations and structure
 
## 📄 License
- This project is open for learning and personal use.
