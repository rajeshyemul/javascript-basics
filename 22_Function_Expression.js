// Function Expression
// A function assigned to a variable

let multiplyNumbers = function (a, b) {
    return a * b;
};

console.log(multiplyNumbers(5, 4)); // 20

/*
========================
Key Learning Points
========================
1. Function expressions are NOT hoisted.
2. They must be defined before calling.
3. Functions are treated as values and stored in variables.
*/
